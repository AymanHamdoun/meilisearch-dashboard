services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-app-container
    volumes:
      - ./:/app
    ports:
      - "3000:80"
    env_file:
      - .env